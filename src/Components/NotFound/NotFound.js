import React from 'react';
import Navbar from '../Shared/Navbar/Navbar';

const NotFound = () => {
    return (
        <section>
            <Navbar/>
            404 not found
        </section>

    );
};

export default NotFound;